# 1-6 Step 5: 빌드 검증 + 학습 리뷰

> **상위 계획**: `docs/plan/phase-1-step6-past-exam-list.md` Step 5 섹션
> **선행 완료**: Step 1 (Zod 필터 스키마), Step 2 (Server Actions), Step 3 (DataTable UI), Step 4 (서버사이드 페이지네이션)
> **작성일**: 2026-02-19
> **상태**: ✅ 완료 (2026-02-19)

---

## 1. 목표

Step 1~4에서 구현한 기출문제 조회 [F006] 기능 전체를 **빌드 레벨에서 통합 검증**하고, 사용자가 이번 스텝 전체에서 배운 핵심 개념들을 **종합 학습 리뷰**로 정리한다.

### 범위

| 포함 | 제외 |
|------|------|
| 전체 테스트 실행 (vitest) | 새로운 코드 작성 |
| 린트 검증 (eslint) | 새로운 테스트 추가 |
| 프로덕션 빌드 검증 (next build) | 기능 수정/확장 |
| Step 1~4 학습 리뷰 종합 | E2E 테스트 (Playwright) |
| 빌드 에러 수정 (발생 시) | |

---

## 2. 빌드 검증 절차

### Phase 1: 테스트 실행

#### 2-1. 1-6 관련 테스트 개별 실행

```bash
# Step 1: Zod 필터 스키마 테스트 (29개)
npx vitest run src/lib/validations/__tests__/past-exams-filter.test.ts

# Step 2: Server Actions 목록/상세 조회 테스트 (18개)
npx vitest run src/lib/actions/__tests__/past-exams-list.test.ts

# Step 2: 기존 업로드 테스트 회귀 확인 (9개)
npx vitest run src/lib/actions/__tests__/past-exams.test.ts
```

**기대 결과**: 전체 PASS, 실패 0개

**실패 시 대응**:
1. 실패한 테스트 로그 확인
2. 테스트 코드 vs 구현 코드 비교
3. 구현 코드 수정 (테스트가 맞다면 구현 수정, 테스트가 틀리면 테스트 수정)
4. 재실행하여 PASS 확인

#### 2-2. 전체 테스트 실행

```bash
npx vitest run
```

**기대 결과**: 프로젝트 전체 테스트 PASS (기존 테스트 회귀 없음)

**확인 포인트**:
- 1-6에서 추가한 테스트가 기존 테스트에 영향을 주지 않는지
- Mock 설정이 다른 테스트 파일과 충돌하지 않는지 (별도 파일 분리로 해결되었어야 함)

### Phase 2: 린트 검증

```bash
npm run lint
```

**기대 결과**: 에러 0개

**주의할 lint 이슈들**:
- `@next/next/no-img-element`: `past-exam-detail-sheet.tsx`에서 `eslint-disable-next-line` 처리됨
- `@typescript-eslint/no-explicit-any`: `past-exams.ts`에서 Supabase 타입 관련 `eslint-disable` 처리됨
- 미사용 import 또는 변수가 있으면 수정

**실패 시 대응**:
1. 에러 목록 확인
2. 파일별로 수정 (auto-fix 가능한 것은 `npm run lint -- --fix`)
3. 재실행하여 에러 0개 확인

### Phase 3: 프로덕션 빌드

```bash
npm run build
```

**기대 결과**: 빌드 성공 (exit code 0)

**빌드에서 검증되는 항목들**:
| 검증 항목 | 대상 파일 | 설명 |
|----------|----------|------|
| TypeScript 타입 체크 | 전체 | `PastExamListItem`, `PastExamDetail` 등 타입 호환성 |
| Server Component 정합성 | `page.tsx` | `async function` + `await searchParams` 패턴 |
| Client Component 호환 | toolbar, columns, sheet, pagination | `'use client'` 선언 + hooks 사용 |
| Import 경로 | 전체 | `@/` 별칭 해석, 상호 참조 순환 없음 |
| Next.js 빌드 최적화 | 전체 | 페이지 라우트 생성, 번들 크기 |

**주요 빌드 에러 예상 시나리오 및 대응**:

| # | 예상 에러 | 원인 | 대응 |
|---|----------|------|------|
| 1 | `Type error: Property 'xxx' does not exist` | Supabase placeholder 타입 `as any` 누락 | 해당 위치에 타입 캐스팅 추가 |
| 2 | `Module not found` | Import 경로 오류 (특히 `@/components/data-table` 인덱스) | `index.ts` export 확인 |
| 3 | `'use client' directive required` | Client Component에서 hooks 사용하는데 directive 누락 | 파일 상단에 `'use client'` 추가 |
| 4 | `Async Server Component` 관련 경고 | `searchParams` Promise 처리 | `await searchParams` 패턴 확인 |
| 5 | `Cannot find module '@/lib/actions/past-exams'` | 순환 참조 또는 export 누락 | Named export 확인 |

### Phase 4: 수동 검증 (선택사항, 로컬 서버 구동 가능 시)

```bash
npm run dev
```

**수동 검증 체크리스트**:
- [ ] `/past-exams` 접속 → DataTable 렌더링
- [ ] 학교명 입력 → debounce 후 URL 변경 + 데이터 갱신
- [ ] 과목 입력 → debounce 후 URL 변경 + 데이터 갱신
- [ ] 학년/시험유형/연도/학기 Select → 즉시 URL 변경 + 데이터 갱신
- [ ] 복합 필터 → 여러 필터 조합 동작
- [ ] "상세" 버튼 → Sheet 열림 + 메타데이터 표시
- [ ] Sheet에서 이미지 미리보기 (Signed URL)
- [ ] 페이지네이션 버튼 → `?page=N` URL 변경 + 데이터 갱신
- [ ] 필터 적용 상태에서 페이지 이동 → 필터 보존
- [ ] 브라우저 뒤로가기 → 이전 상태 복원
- [ ] 교사/관리자 계정 → "기출문제 업로드" 버튼 표시
- [ ] 학생 계정 → "기출문제 업로드" 버튼 미표시
- [ ] 데이터 0건 → "등록된 기출문제가 없습니다." 메시지

---

## 3. 빌드 에러 수정 가이드라인

빌드 에러가 발생할 경우 아래 원칙을 따른다:

### 수정 원칙
1. **최소 변경**: 에러를 해결하는 최소한의 수정만 적용
2. **기능 변경 금지**: 빌드 수정은 타입 오류, import 경로 등 기술적 문제만 해결
3. **테스트 재실행**: 수정 후 반드시 `npx vitest run` 재실행하여 회귀 확인
4. **단일 파일 수정**: 가능하면 한 파일씩 수정하고 빌드 재시도

### 수정 범위 제한
- 타입 캐스팅 추가/수정
- Import 경로 수정
- `'use client'` / `'use server'` directive 추가
- ESLint disable 주석 추가
- **새로운 기능/로직 추가는 하지 않음**

---

## 4. 학습 리뷰: Step 1~4 종합

### 4-1. 개념 목록 (등급별)

| # | 개념 | 등급 | Step | 관련 파일 |
|---|------|------|------|----------|
| 1 | Supabase FK JOIN (`schools!inner`, `profiles!uploaded_by`) | 🔴 | Step 2 | `src/lib/actions/past-exams.ts` |
| 2 | Signed URL (Storage 보안 — 경로 저장 + 조회 시 생성) | 🔴 | Step 2 | `src/lib/actions/past-exams.ts` |
| 3 | useEffect race condition 방지 (`let cancelled = false`) | 🟡 | Step 3 | `past-exam-detail-sheet.tsx` |
| 4 | 다중 필터 URL 상태 관리 (6개 필터 + searchParams) | 🟡 | Step 3 | `past-exams-toolbar.tsx` |
| 5 | 서버사이드 페이지네이션 (URL searchParams + 필터 보존) | 🟡 | Step 4 | `data-table-server-pagination.tsx` |
| 6 | sanitizeFilters 전처리 (빈 문자열 → undefined) | 🟡 | Step 2 | `src/lib/actions/past-exams.ts` |
| 7 | 업로드 스키마 vs 필터 스키마 (엄격 vs 관대) | 🟢 | Step 1 | `src/lib/validations/past-exams.ts` |
| 8 | 정적 컬럼 배열 vs 팩토리 함수 | 🟢 | Step 3 | `past-exam-columns.tsx` |
| 9 | `<img>` vs `next/image` (Signed URL 사용 시) | 🟢 | Step 3 | `past-exam-detail-sheet.tsx` |
| 10 | camelCase 변환 + FK JOIN 중첩 객체 추출 | 🟢 | Step 2 | `src/lib/actions/past-exams.ts` |

### 등급 설명
- **🔴 새 패턴 / 보안**: 반드시 이해해야 함. 삭제 후 재구현 제안 대상
- **🟡 유틸리티 / 상태관리**: 재구현 권장 (빈칸채우기 등)
- **🟢 반복 패턴 / UI**: AI 자동 구현 OK. 개념 확인만

---

### 4-2. 핵심 개념 상세 설명 + 이해도 질문

#### 개념 1: Supabase FK JOIN (🔴)

**무엇인가**:
Supabase(PostgREST)에서 외래 키(FK) 관계를 활용하여 여러 테이블의 데이터를 한 번의 쿼리로 가져오는 문법.

**왜 중요한가**:
- SQL의 `JOIN`을 직접 쓰지 않고도 관련 테이블 데이터를 한 번에 조회 가능
- `N+1 문제`를 방지 (목록의 각 행마다 별도 쿼리를 보내지 않음)
- 기출문제에서는 `schools` 테이블의 학교명과 `profiles` 테이블의 업로드자 이름을 JOIN으로 가져옴

**핵심 문법 구분**:
```
schools!inner (name, school_type)
  │       │
  │       └─ JOIN 방식: INNER JOIN (학교 없는 기출문제 제외)
  └─ FK 대상 테이블명

profiles!uploaded_by (name)
  │         │
  │         └─ FK 컬럼명: uploaded_by (같은 테이블 FK가 2개 이상일 때 구분자)
  └─ FK 대상 테이블명
```

`!` 뒤에 오는 값:
- `inner` = JOIN 방식 지정 (INNER JOIN)
- `uploaded_by` = FK 컬럼명 지정 (어떤 FK를 사용할지 명시)
- 둘 다 동시에 사용 가능: `profiles!uploaded_by!inner`

**반환 형태** (PostgREST JSON):
```json
{
  "id": "exam-uuid",
  "year": 2024,
  "schools": { "name": "한국고등학교", "school_type": "high" },
  "profiles": { "name": "김교사" }
}
```
관련 테이블 데이터가 중첩 객체로 반환됨 → `toPastExamListItem`에서 `dbRow.schools?.name`으로 추출.

**이해도 질문 1**:
> `schools!inner`에서 `!inner`를 빼고 `schools`만 쓰면 어떤 차이가 생기는가? 학교가 삭제된 기출문제가 있다면 결과에 어떤 영향을 미치는가?

**이해도 질문 2**:
> `profiles!uploaded_by`에서 `!uploaded_by`를 빼고 `profiles`만 쓰면 왜 에러가 발생할 수 있는가? 힌트: `past_exam_questions` 테이블에서 `profiles` 테이블을 참조하는 FK가 몇 개인지 생각해보라.

---

#### 개념 2: Signed URL (🔴)

**무엇인가**:
Supabase Storage에 저장된 파일에 **일시적으로 접근할 수 있는 URL**을 서버에서 생성하는 보안 패턴.

**왜 중요한가**:
- DB에는 **파일 경로만** 저장 (예: `academy-id/school-id/2024-1-midterm/file.jpg`)
- URL을 직접 저장하면: 누구나 URL을 알면 파일에 접근 가능 (보안 위험)
- Signed URL은 **만료 시간**이 있어서 일정 시간 후 자동 무효화
- **생성 시점**: 상세 조회(`getPastExamDetail`) 시에만 생성 (목록 조회에서는 불필요)

**흐름**:
```
[업로드 시]
  파일 → Storage 업로드 → 경로(path)만 DB에 저장

[조회 시]
  DB에서 경로 조회 → createSignedUrl(path, 60) → 60초짜리 URL 반환 → 클라이언트에 전달
```

**코드**:
```typescript
if (row.source_image_url) {
  const { data: signedData } = await supabase.storage
    .from('past-exams')          // 버킷 이름
    .createSignedUrl(
      row.source_image_url,      // Storage 경로
      60                         // 만료 시간 (초)
    )
  signedImageUrl = signedData?.signedUrl ?? null
}
```

**이해도 질문 3**:
> 만약 DB에 Signed URL 자체를 저장한다면 어떤 문제가 발생하는가? (보안 관점과 실용 관점 모두)

**이해도 질문 4**:
> 목록 조회(`getPastExamList`)에서는 Signed URL을 생성하지 않고, 상세 조회(`getPastExamDetail`)에서만 생성하는 이유는 무엇인가? 성능 관점에서 설명하라.

---

#### 개념 3: useEffect race condition 방지 (🟡)

**무엇인가**:
비동기 작업(데이터 패칭)을 수행하는 `useEffect`에서, 컴포넌트가 언마운트되거나 의존성이 변경될 때 **이전 요청의 결과가 현재 상태를 덮어쓰는 문제**를 방지하는 패턴.

**왜 중요한가**:
- Sheet를 빠르게 열었다 닫았다 다시 열면, 이전 Sheet의 응답이 늦게 도착하여 현재 Sheet의 데이터를 덮어쓸 수 있음 (Last Write Wins 문제)
- Server Action은 `AbortController`를 지원하지 않으므로, `let cancelled` 플래그로 직접 처리

**패턴**:
```typescript
useEffect(() => {
  if (!open || !examId) return

  let cancelled = false         // 1. 플래그 선언

  setLoading(true)
  getPastExamDetail(examId)
    .then((result) => {
      if (cancelled) return     // 2. 결과 도착 시 체크
      setDetail(result.data)
    })
    .finally(() => {
      if (!cancelled) setLoading(false)
    })

  return () => {
    cancelled = true            // 3. cleanup에서 무효화
  }
}, [open, examId])
```

**이해도 질문 5**:
> `let cancelled = false` 대신 `AbortController`를 사용할 수 없는 이유는 무엇인가? 만약 Server Action이 아닌 일반 `fetch`였다면 어떻게 달라지는가?

---

#### 개념 4: 다중 필터 URL 상태 관리 (🟡)

**무엇인가**:
6개의 필터 값을 URL의 `searchParams`로 관리하여, 북마크/공유/뒤로가기가 자연스럽게 동작하도록 하는 패턴.

**핵심 동작**:
1. 필터 변경 → `URLSearchParams` 객체에 반영
2. 값이 기본값('all' 또는 빈 문자열)이면 → `params.delete(key)` (URL 깔끔하게)
3. `params.delete('page')` → 필터 변경 시 항상 1페이지로 리셋
4. `router.push()` → Server Component 재렌더링 → 새 데이터 조회

**텍스트 Input vs Select 차이**:
- **Input** (학교명, 과목): `useState`로 로컬 상태 + `useEffect` 내 `setTimeout` 300ms debounce
- **Select** (학년, 시험유형, 연도, 학기): `onValueChange`에서 즉시 `router.push()` (debounce 불필요)

**이해도 질문 6**:
> 필터 변경 시 `params.delete('page')`를 하는 이유는 무엇인가? 만약 이 줄을 빼면 사용자 경험에 어떤 문제가 생기는가?

---

#### 개념 5: 서버사이드 페이지네이션 (🟡)

**무엇인가**:
전체 데이터를 한 번에 가져오지 않고, URL의 `?page=N` 파라미터에 따라 서버에서 해당 페이지 데이터만 가져오는 방식. 클라이언트 사이드 페이지네이션과 대비됨.

**클라이언트 vs 서버 페이지네이션**:
| 구분 | 클라이언트 사이드 | 서버 사이드 |
|------|-----------------|------------|
| 데이터 전송 | 전체 데이터 한 번에 | 페이지당 10건만 |
| 적합한 상황 | 소량 데이터 (100건 이하) | 대량 데이터 (1000건+) |
| 구현 위치 | TanStack Table `getPaginationRowModel()` | URL searchParams + Server Action `range()` |
| 상태 관리 | Table 내부 상태 | URL searchParams |

**구현 시 핵심 포인트**:
- `DataTable`의 기본 `DataTablePagination`을 `showPagination={false}`로 비활성화
- `DataTableServerPagination` 공용 컴포넌트를 DataTable 외부에 배치
- `page=1`이면 URL에서 `page` 파라미터 삭제 (깔끔한 URL 관례)

**이해도 질문 7**:
> `DataTable`의 기본 `DataTablePagination`(클라이언트 사이드)을 그대로 사용하면, 서버가 10건만 반환했을 때 어떤 문제가 발생하는가?

---

#### 개념 6: sanitizeFilters 전처리 (🟡)

**무엇인가**:
URL `searchParams`에서 빈 문자열(`''`)이 넘어올 때, Zod 파싱 전에 `undefined`로 변환하는 헬퍼 함수.

**왜 필요한가**:
- URL `?school=&grade=`에서 `school`과 `grade`는 빈 문자열(`''`)
- `z.string().optional()`에 `''` → 빈 문자열이 통과 (의도하지 않은 ilike '%''%' 필터링)
- `z.coerce.number()`에 `''` → `0`으로 변환 → `.min(1)` 실패 → Zod 에러

**전처리 위치 결정 근거**:
- Zod 스키마 자체에 넣으면 (`z.preprocess`): 스키마 복잡도 증가
- page.tsx에서 처리하면: Server Action 단독 호출 시 방어 안 됨
- **Server Action 내부, Zod 파싱 직전**: 가장 안전한 위치 (Defense in Depth)

**이해도 질문 8**:
> `sanitizeFilters`를 page.tsx(Server Component)에서 호출하지 않고, Server Action 내부에서 호출하는 이유를 "Defense in Depth" 관점에서 설명하라.

---

### 4-3. 패턴 비교 종합표 (Step 1~4 전체)

| 설계 결정 | 선택한 방안 | 대안 | 선택 이유 |
|----------|-----------|------|----------|
| 컬럼 정의 방식 | 정적 배열 | 팩토리 함수 | 권한별 분기 없음 (조회 전용) |
| Sheet 데이터 패칭 | `examId`만 전달 + 열 때 조회 | 목록 데이터 전달 + 추가 조회 | Signed URL은 목록에 없음 → 상세 조회 필수 |
| 이미지 렌더링 | `<img>` | `next/image` | Signed URL은 외부+동적 → remote patterns 설정 복잡 |
| 빈 문자열 처리 | sanitizeFilters 헬퍼 | z.preprocess | 스키마 깔끔 유지, Action 내부 처리 |
| 페이지네이션 | 서버사이드 (별도 컴포넌트) | 클라이언트 (TanStack Table 내장) | 대량 데이터, URL 기반 상태 |
| getCurrentUserProfile | 복사 (past-exams.ts 내) | 공통 모듈 추출 | 3회 반복 규칙 미달 (2회), MVP 범위 최소화 |
| 테스트 파일 | 별도 파일 (past-exams-list.test.ts) | 기존 파일에 추가 | Mock 패턴 충돌 방지 (upload=dynamic import, list=동기) |

---

### 4-4. Mock 테스트의 한계 (중요)

Step 2에서 작성한 18개 테스트는 **Supabase 클라이언트를 Mock**으로 대체한다. 이는 다음을 검증하지 못한다:

| 검증 가능 | 검증 불가 |
|----------|----------|
| 함수 호출 순서 | 실제 SQL 쿼리 결과 |
| 인자 전달 정확성 | FK JOIN이 실제로 동작하는지 |
| 에러 핸들링 분기 | 컬럼명 오타 (`subjects` vs `subject`) |
| camelCase 변환 로직 | RLS 정책 적용 여부 |
| 반환 타입 구조 | 실제 데이터 정합성 |

**교훈**: Mock 테스트 PASS ≠ 실제 동작 보장. `npm run build` + 수동 E2E 검증이 보완적으로 필요. 향후 Playwright E2E 테스트 추가 시 이 갭을 해소할 수 있다.

---

## 5. 직접 구현 추천 판단

| 개념 | 등급 | 추천 |
|------|------|------|
| Supabase FK JOIN | 🔴 | **빈칸채우기 재구현 권장** — `getPastExamDetail`의 FK JOIN 쿼리 부분을 빈칸으로 만들어 직접 작성 |
| Signed URL | 🔴 | **빈칸채우기 재구현 권장** — `createSignedUrl` 호출 부분을 빈칸으로 만들어 직접 작성 |
| useEffect race condition | 🟡 | Step 3에서 빈칸채우기 완료 ✅ — 추가 재구현 불필요 |
| 다중 필터 URL 관리 | 🟡 | 1-7 이후 유사 패턴 반복 시 재구현 고려 |
| 서버사이드 페이지네이션 | 🟡 | `goToPage` 함수의 URL 조작 로직 빈칸채우기 고려 |
| sanitizeFilters | 🟡 | 짧은 함수 → 삭제 후 직접 작성 권장 (5줄) |
| 정적 컬럼 vs 팩토리 | 🟢 | AI 구현 OK — 개념 이해만 확인 |
| `<img>` vs `next/image` | 🟢 | AI 구현 OK — 이유만 이해 |

---

## 6. 파일 변경 요약 (Step 5)

### 예상 변경: 빌드 에러 없으면 0개

빌드 에러 발생 시에만 최소 수정. 새로운 파일 생성 없음.

### Step 1~4 전체 변경 파일 목록 (참조용)

#### 수정 (4개)
| 파일 | Step | 변경 |
|------|------|------|
| `src/lib/validations/past-exams.ts` | 1 | `pastExamFilterSchema` 추가 |
| `src/lib/actions/past-exams.ts` | 2 | `getPastExamList`, `getPastExamDetail` + 타입 + 헬퍼 |
| `src/app/(dashboard)/past-exams/page.tsx` | 3,4 | placeholder → DataTable + ServerPagination |
| `src/components/data-table/index.ts` | 4 | `DataTableServerPagination` export 추가 |

#### 신규 (7개)
| 파일 | Step | 설명 |
|------|------|------|
| `src/lib/validations/__tests__/past-exams-filter.test.ts` | 1 | 필터 스키마 테스트 (29개) |
| `src/lib/actions/__tests__/past-exams-list.test.ts` | 2 | 조회 테스트 (18개) |
| `src/app/(dashboard)/past-exams/_components/constants.ts` | 3 | UI 상수 매핑 |
| `src/app/(dashboard)/past-exams/_components/past-exam-columns.tsx` | 3 | 컬럼 정의 (9개) |
| `src/app/(dashboard)/past-exams/_components/past-exams-toolbar.tsx` | 3 | 6개 필터 Toolbar |
| `src/app/(dashboard)/past-exams/_components/past-exam-detail-sheet.tsx` | 3 | 상세 Sheet + Signed URL |
| `src/components/data-table/data-table-server-pagination.tsx` | 4 | 서버사이드 페이지네이션 |

**총: 4개 수정 + 7개 신규 = 11개 파일**

---

## 7. 성공 기준

### 빌드 검증
- [x] `npx vitest run` — 전체 테스트 PASS (347 tests, 21 files)
- [x] `npm run lint` — 에러 0개 (경고 5개 — 기존, 1-6 무관)
- [x] `npm run build` — 빌드 성공 (Next.js 16.1.6, `/past-exams` 라우트 생성)

### 학습 리뷰
- [x] 이해도 질문 8개에 대해 사용자 답변 완료
- [x] 🔴 개념 (FK JOIN, Signed URL) 빈칸채우기 재구현 여부 결정
- [x] 다음 단계 (1-7 기출 기반 AI 문제 생성) 진행 준비 확인

---

## 8. 구현 순서 요약

```
Phase 1: 테스트 실행
  → npx vitest run (개별 → 전체)
  → 실패 시 수정 → 재실행

Phase 2: 린트 검증
  → npm run lint
  → 에러 시 수정

Phase 3: 프로덕션 빌드
  → npm run build
  → 에러 시 수정 (최소 변경)
  → 빌드 성공 확인

Phase 4: 수동 검증 (선택)
  → npm run dev → /past-exams 접속 → 기능 확인

Phase 5: 학습 리뷰
  → 핵심 개념 10개 설명
  → 이해도 질문 8개 (사용자 답변 대기)
  → 직접 구현 추천 판단

Phase 6: 1-6 완료 처리
  → HANDOFF.md 업데이트 (1-6 완료)
  → ROADMAP.md 업데이트 (1-6 ✅)
  → 상위 계획 docs/plan/phase-1-step6-past-exam-list.md 업데이트 (5/5 완료)
  → MEMORY.md 업데이트 (기술 교훈 추가)
  → 커밋·푸시
```

---

## 9. 다음 단계 안내

Step 5 완료 후:
- **1-6 기출문제 조회 [F006]** 전체 완료 (5/5 Steps)
- **다음**: 1-7 기출 기반 AI 문제 생성 [F011] 계획 수립
- 0-5에서 구축한 AI 추상화 레이어 (Factory + Strategy, GeminiProvider) 활용
