# ===========================================
# Compass 환경변수 설정
# ===========================================
# 이 파일을 .env.local로 복사한 후 실제 값을 입력하세요.
# cp .env.example .env.local

# -------------------------------------------
# Supabase
# -------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=           # Supabase 프로젝트 URL (필수)
NEXT_PUBLIC_SUPABASE_ANON_KEY=      # Supabase Anon Key (필수)
SUPABASE_SERVICE_ROLE_KEY=          # Supabase Service Role Key (필수, 서버 전용)

# -------------------------------------------
# AI 서비스
# -------------------------------------------
GEMINI_API_KEY=                     # Google Gemini API 키 (필수)
GEMINI_MODEL=gemini-2.0-flash      # Gemini 모델 (선택, 기본: gemini-2.0-flash)
AI_PROVIDER=gemini                  # AI 제공자 (선택, 기본: gemini)
AI_MAX_RETRIES=3                    # 최대 재시도 횟수 (선택, 기본: 3, 범위: 1-10)
AI_TIMEOUT_MS=30000                 # API 타임아웃 ms (선택, 기본: 30000, 범위: 1000-120000)
